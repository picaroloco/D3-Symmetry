[package]
name = "d3-symmetry"
version = "0.1.0"
edition = "2021"

[dependencies]
# Python integration for Z3 verification
pyo3 = { version = "0.22", features = ["auto-initialize"] }

# Existing crypto dependencies
num-bigint = "0.4"
num-traits = "0.2"
num-integer = "0.1"
